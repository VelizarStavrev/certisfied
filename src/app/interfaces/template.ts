export interface Template {
    data: {
        id: string,
        name: string, 
        created: string, 
        edited: string, 
        notes: string
    },
    message: string,
    status: boolean
}
